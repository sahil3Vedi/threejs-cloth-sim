export class ClothMaterial {
    areaDensity: number;
    springiness: number;
    detail: number;

    constructor(areaDensity: number, springiness: number, detail: number){
        this.areaDensity = areaDensity;
        this.springiness = springiness;
        this.detail = detail;
    }
}